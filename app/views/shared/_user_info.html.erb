<%= link_to gravatar_for(current_user, size: Settings.digit_50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("view_if"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
